# Configure GIT
#configure user name
$ git config --global user.name "gcs2k5"

#configure email address
$ git config --global user.email "gcs2k5@gmail.com"

#What we config in Git
$ git config --list

# To clone from remote to local
git clone https://github.com/gcs2k5/Git-repo.git


#git status is used to see the status
git status

#git status types
 u = untracked = new file that git did not track
 m = modified = changed
 s= staged = file is ready for commit
 unmodified = unchanged

 #git add :- add new or changed file in your working directory to the git staging area
 git add <filename>

 #if there are multile file and we use git dot (.) command
 git add .    

#git commit :- it is used to record the change
git commit -m "some message"


#to update all the modification from git to into git hub
  git push origin main
=====================================================================
#if we start creating a folder from local machine laptop and want to push code into git hub we need to use "init" command

git init  (To create a git repo in local)
git status 
git add .
git commmit -m "modify"
git remote add origin <link>
git remote -v (to verify remote)
git branch (to check the branch)
git branch -M main (to rename branch from master or anyname  to main)
git push origin main
git push -u origin  (to set the orginin, mean after run this command we don't need to write full command we can enter below command)
git push 

#Work flow of Local Git
Github repo-->clone -->changes-->add-->commit-->push

# To manage Git branch there are few commands
Git branch (To check the branch)
git branch -M main (To rename the brach)
git checkout <-branchname>
git checkout -b <-new branch name> #to create new branch
git branch -d <-brach name> #To delete the branch
#create a new brach
git checkout -b feature1
make a changes in new or existing file
git add .
git status
git commit -m "add new tag"
git push origin feature1 (to save modification into feature1)

#after push when we check our repo and check feature1 it will have modification but main branch do not have modification
git diff main ( this command is used To check difference with main branch i.e. you should have exist modfiy branch)
#Now to merge difference using graphically
#go to git hub and full feature1 and merge changes
#after that if we want to let come changes from remote to main

git pull origin main (To match the contains)


got to git 

################### Merge conflct #############
#if there is same modification in the same line then
#got to main brach and make changes in html file and save it
git push .
git commit -m "add button"
git checkout f2 #Now we need to make changes in index.html" after that run below commands
git push .
git commit -m "add dropdown"
git diff main #To check the difference in f2 and main brach
git merge main #To merge the differences b/w f2 and main brach#
git status
git add .
git commit -m "Merge both feature or conflct"
go to main branch and run merge
git merge f2

 





#Undoing changes
case 1 # staged changes
get reset <-file name>
git reset

Case 2 : commited changes (for one comming)
 git reset HEAD-1

 Case3: commited changes (for many commits)
  git reset <-commit hash]>
  git reset --hard <-commit hash>